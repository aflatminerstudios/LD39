/// @func scrLowPower(cabinetId)
/// @desc Put cabinet in low power mode, right now just turns off
/// @param {id} cabinetID The id of the cabinet to put in low power

var cabinetID = argument0;

scrToggleCabinet(cabinetID);

